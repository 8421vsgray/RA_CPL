# --- 核心深度学习框架 ---
# 建议安装支持 CUDA 的版本以加速 200 轮训练
torch>=1.12.0
torchvision>=0.13.0

# --- 科学计算与数据处理 ---
numpy>=1.22.0
scipy>=1.7.0
pandas>=1.4.0

# --- 聚类指标与机器学习工具 ---
# 包含 KMeans, NMI, ARI 等核心评估算法
scikit-learn>=1.0.0

# --- 配置管理与实用工具 ---
# 用于加载 YAML 格式的配置文件
pyyaml>=6.0
# 用于像对象一样访问字典（cfg.training.lr）
easydict>=1.9

# --- 可视化 (可选，用于画 Loss 曲线) ---
matplotlib>=3.5.0